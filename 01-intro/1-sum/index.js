/**
 * sum
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
export default function sum(m, n) {
    return m + n;
}
 let result = sum(1,1);
 alert (result);
 result = sum(2,5);
 alert (result);